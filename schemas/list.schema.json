{
    "title": "list test",
    "description": "json validate test",
    "type": "object",
    "required": ["values"],
    "properties": {
        "values": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^aaa|bbb$"
          }
        }
    }
}